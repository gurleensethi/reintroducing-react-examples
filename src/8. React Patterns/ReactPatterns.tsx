import React, { FunctionComponent } from "react";
import { CompoundComponents } from "./CompoundComponents";

export const ReactPatterns: FunctionComponent = (props) => {
  return (
    <div>
      <h1>
        <u>Advance React Patterns</u>
      </h1>
      <hr />
      <CompoundComponents />
    </div>
  );
};
